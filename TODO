ComplierProj1
0. 学习lr分析法
    (LA)LR是否允许出现下列二义文法(同一个串可以规约成两种非终结符 即规约规约冲突)
    Exp1 -> int | -Exp1 | Exp1 - Exp1
    Exp2 -> int | -Exp2 | Exp2 - SEMI

    "-1-;" 
1. 错误恢复
2. minus语义的二义性: 通过指定结合性, 解决对表达式取负右结合 减法左结合的问题.
3. 默认:
    规约规约冲突先规约在前面的产生式 移入规约冲突选择移入.
    如果发生冲突, 则直接弹栈+再同步, 不会回溯.
4. 实验括号不必定义结合性优先级(不会有二义性)(不重要)

ComplierProj2
0. 存在几处内存没有释放（不影响程序结果）。
    一是Exp-> Exp ASSIGNOP Exp 产生式中，发生错误时应当加上deletetype（）。
    没发生错误应该返回expTypeLeft而不是copyType（expTypeLeft）（%%Exp其他产生式
    也可能有类似问题）

    二是VarDec， 当添加结构体类型时，创建的符号表条目不必插入符号表，但没有
    使用后没有释放条目所占这部分空间。


